---
export const prerender = false
import { getData } from 'lib/fetch';
import Layout from '../layouts/Layout.astro';
import CalendarApp from '@/components/calendario';

const eventos = await getData("actividads")
---

<Layout>
    <main class="min-h-screen bg-background py-16">
        <div class="container mx-auto max-w-6xl px-6">
            <!-- Header -->
            <div class="text-center mb-12">
                <h1 class="text-4xl font-bold mb-4 font-raleway">
                    Calendario CDES
                </h1>
                <p class="text-muted-foreground text-lg font-noto">
                    Eventos, reuniones y actividades del Consejo para el Desarrollo Estratégico de Santiago
                </p>
            </div>

            <!-- Calendario Component -->
            <CalendarApp eventos={eventos} client:only />
        </div>
    </main>
</Layout>

